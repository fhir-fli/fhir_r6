name: fhir_r6_validation
description: >-
  StructureDefinition-driven validation for FHIR R6 resources: structure,
  cardinality, value set bindings, and invariants.
version: 0.5.1
homepage: https://www.fhirfli.dev/
repository: https://github.com/fhir-fli/fhir_r6

topics:
  - fhir
  - hl7
  - healthcare
  - validation
  - interoperability

# This package supports all platforms listed below.
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.5.0

dependencies:
  collection: ^1.19.1
  fhir_r6: ^0.5.0
  fhir_r6_path: ^0.5.0
  http: ^1.6.0

dev_dependencies:
  very_good_analysis: ^10.0.0
  test: ^1.29.0
