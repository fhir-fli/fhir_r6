name: fhir_r6_cds_hooks
description: CDS Hooks 2.0 client and service support for FHIR R6.
version: 0.5.0
homepage: https://www.fhirfli.dev/
repository: https://github.com/fhir-fli/fhir_r6

# This package supports all platforms listed below.
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.5.0

dependencies:
  fhir_r6: ^0.5.0
  http: ^1.6.0

dev_dependencies:
  test: ^1.29.0
  very_good_analysis: ^7.0.0
